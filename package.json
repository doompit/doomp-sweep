{
  "name": "doomps-sweep",
  "version": "3.0.0",
  "description": "Sweep or be Swept — the DOOMPS game, admin panel, and API, all in one service at sweep.doomps.xyz. SQLite on a persistent disk, zero external DB dependency.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.4.0"
  }
}
